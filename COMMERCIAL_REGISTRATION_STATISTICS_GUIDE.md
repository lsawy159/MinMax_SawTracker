# نظام إحصائيات السجل التجاري - دليل الاستخدام

## نظرة عامة
تم تطوير نظام شامل لإحصائيات السجل التجاري في نظام Saw Tracker يوفر عرضاً تفاعلياً بالألوان لحالة السجل التجاري للمؤسسات.

## المزايا الرئيسية

### 1. نظام الألوان التفاعلي
- **🔴 أحمر**: أقل من 30 يوم متبقي أو منتهي الصلاحية
- **🟡 أصفر**: 30-60 يوم متبقي
- **🔵 أزرق**: أكثر من 60 يوم متبقي (ساري)

### 2. عرض المعلومات
- حساب دقيق للأيام المتبقية
- نص واضح: "باقي X يوم" أو "منتهي منذ X يوم"
- أيقونات توضيحية لكل حالة
- تواريخ الانتهاء بالتفصيل

### 3. نظام الإحصائيات
- عرض إجمالي المؤسسات
- إحصائيات مفصلة لكل حالة
- نسب مئوية للحالات المختلفة
- ملخص مرئي بالألوان

## الملفات المحدثة

### 1. `/src/utils/commercialRegistration.ts`
**الدوال المساعدة الجديدة:**
- `calculateDaysRemaining()`: حساب الأيام المتبقية
- `getStatusColor()`: تحديد لون الحالة
- `getStatusText()`: نص الحالة الموضح
- `getStatusIcon()`: أيقونة الحالة
- `getStatusCategory()`: تصنيف الحالة المبسط
- `calculateCommercialRegStats()`: حساب الإحصائيات
- `getStatusColorForFilters()`: لون للفلاتر
- `getShortStatusText()`: نص مختصر للجداول

### 2. `/src/components/companies/CompanyCard.tsx`
**التحسينات المضافة:**
- مربع توضيحي ملون لحالة السجل التجاري
- أيقونات واضحة لكل حالة
- عرض تفصيلي مع التاريخ
- حدود ملونة حسب الحالة

### 3. `/src/pages/Companies.tsx`
**المزايا الجديدة:**
- قسم إحصائيات شامل
- فلاتر محسنة لحالة السجل التجاري
- ترتيب حسب الحالة
- عرض مرئي للإحصائيات

## كيفية الاستخدام

### عرض بطاقة المؤسسة
```tsx
// في CompanyCard.tsx
const commercialRegDays = company.commercial_registration_expiry 
  ? calculateDaysRemaining(company.commercial_registration_expiry)
  : null

<div className={`px-4 py-3 rounded-lg text-sm font-medium border-2 ${getStatusColor(commercialRegDays).backgroundColor} ${getStatusColor(commercialRegDays).textColor} ${getStatusColor(commercialRegDays).borderColor}`}>
  <div className="flex items-center gap-2">
    <span className="text-lg">{getStatusIcon(commercialRegDays)}</span>
    <div className="flex flex-col">
      <span className="font-bold">{getStatusText(commercialRegDays)}</span>
      <span className="text-xs opacity-75">تاريخ الانتهاء: {company.commercial_registration_expiry}</span>
    </div>
  </div>
</div>
```

### حساب الإحصائيات
```tsx
// في Companies.tsx
const stats = calculateCommercialRegStats(companies)

<div className="grid grid-cols-2 md:grid-cols-4 gap-4">
  <div className="text-center p-4 bg-blue-50 rounded-lg border border-blue-200">
    <div className="text-2xl font-bold text-blue-700">{stats.valid}</div>
    <div className="text-sm text-blue-600">ساري ({stats.percentageValid}%)</div>
  </div>
  
  <div className="text-center p-4 bg-yellow-50 rounded-lg border border-yellow-200">
    <div className="text-2xl font-bold text-yellow-700">{stats.expiringSoon}</div>
    <div className="text-sm text-yellow-600">قريب الانتهاء ({stats.percentageExpiringSoon}%)</div>
  </div>
  
  <div className="text-center p-4 bg-red-50 rounded-lg border border-red-200">
    <div className="text-2xl font-bold text-red-700">{stats.expired}</div>
    <div className="text-sm text-red-600">منتهي الصلاحية ({stats.percentageExpired}%)</div>
  </div>
</div>
```

### تطبيق الفلاتر
```tsx
// فلتر حالة السجل التجاري
const [commercialRegStatus, setCommercialRegStatus] = useState<CommercialRegStatus>('all')

// تطبيق الفلتر
if (commercialRegStatus !== 'all') {
  filtered = filtered.filter(company => {
    if (!company.commercial_registration_expiry) return false
    const daysRemaining = calculateDaysRemaining(company.commercial_registration_expiry)
    const category = getStatusCategory(daysRemaining)

    if (commercialRegStatus === 'expired') return category === 'expired'
    if (commercialRegStatus === 'expiring_soon') return category === 'expiring_soon'
    if (commercialRegStatus === 'valid') return category === 'valid'
    return true
  })
}
```

## خيارات الفلترة

### حالة السجل التجاري
- **الكل**: عرض جميع المؤسسات
- **منتهي الصلاحية**: أقل من 0 يوم أو أقل من 30 يوم
- **أقل من 60 يوم**: 30-60 يوم متبقي
- **أكثر من 60 يوم**: ساري وأكثر من 60 يوم

### خيارات الترتيب
- حسب الاسم
- حسب نوع المؤسسة
- حسب تاريخ التسجيل
- حسب حالة السجل التجاري
- حسب حالة اشتراك التأمينات
- حسب عدد الموظفين
- حسب حالة اشتراك قوى ومقيم

## التصميم والألوان

### نظام الألوان المتبع
- **أزرق**: للسجلات السارية (أكثر من 60 يوم)
- **أصفر**: للقريبة من الانتهاء (30-60 يوم)
- **أحمر**: للمنتهية أو القريبة جداً (أقل من 30 يوم)

### فئات الحدود
- `border-red-400`: للمؤسسات الحرجة
- `border-yellow-400`: للمؤسسات القريبة من الانتهاء
- `border-blue-400`: للمؤسسات السارية
- `border-gray-200`: للمؤسسات بدون تاريخ

## التحسينات المستقبلية

### 1. التنبيهات التلقائية
- إرسال إشعارات قبل انتهاء السجل بـ 60 يوم
- تذكيرات دورية للمؤسسات المنتهية
- تنبيهات فورية للوضع الحرج

### 2. التقارير المتقدمة
- تقارير شهرية لحالة السجلات
- إحصائيات تفصيلية للمؤسسات
- مؤشرات الأداء الرئيسية (KPIs)

### 3. التصدير والاستيراد
- تصدير قائمة المؤسسات حسب الحالة
- استيراد تواريخ التجديد
- نسخ احتياطية للبيانات

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. عدم عرض التاريخ
- **السبب**: التاريخ غير محدد في قاعدة البيانات
- **الحل**: التحقق من وجود `commercial_registration_expiry` في البيانات

#### 2. حسابات خاطئة للأيام
- **السبب**: مشكلة في تنسيق التاريخ
- **الحل**: التأكد من استخدام صيغة ISO (YYYY-MM-DD)

#### 3. الألوان لا تظهر
- **السبب**: تعارض مع CSS classes أخرى
- **الحل**: التأكد من تحميل Tailwind CSS بشكل صحيح

## الدعم والصيانة

### ملفات المراقبة
- `/src/utils/commercialRegistration.ts`: الدوال الأساسية
- `/src/components/companies/CompanyCard.tsx`: عرض البطاقات
- `/src/pages/Companies.tsx`: صفحة الإدارة والفلاتر

### اختبار النظام
1. تحقق من حساب الأيام المتبقية
2. اختبر الفلاتر المختلفة
3. تأكد من صحة الألوان
4. راجع الإحصائيات

---

**تم تطوير هذا النظام بواسطة فريق تطوير Saw Tracker**  
**آخر تحديث: نوفمبر 2025**
